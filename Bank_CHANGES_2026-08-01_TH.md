# Bank — สรุปงานวันที่ 1 สิงหาคม 2569

ต่อจาก `Bank_CHANGES_TH.md` (31 ก.ค.) — เอกสารนี้เป็นงานของวันถัดมา

---

## สรุปสั้น

วันนี้แก้ไป **11 เรื่อง** โดยแนวคิดหลักคือ **เลิกใช้ข้อมูลปลอม เปลี่ยนมาใช้ฐานข้อมูลจริง**

หน้าที่สมบูรณ์แล้ว (ทุกปุ่มทำงานจริง):
- **Settings** — ธีม, ค่าเกณฑ์, refresh rate, เสียงแจ้งเตือน, อีเมล
- **History** — ตาราง, Export CSV
- **Dashboard** — ทุกตัวเลขมาจากฐานข้อมูล

---

## 1. ตัดตัวเลือกภาษาอังกฤษออก

**ทำไม:** ปุ่ม 🇬🇧 English กดแล้วไม่มีอะไรเกิดขึ้น สร้างความสับสน

การทำระบบสองภาษาจริงต้องรื้อข้อความทุกจุดใน 6 หน้า + Sidebar + Topbar (หลายร้อยจุด) จึงตัดสินใจเหลือภาษาไทยอย่างเดียวไปก่อน

**ไฟล์:** `pages/SettingsPage.tsx` — ลบแถว "ภาษา", ลบ `type Language` และ state ที่ไม่ได้ใช้ (จำเป็น เพราะ `tsconfig` เปิด `noUnusedLocals`)

---

## 2. หน้า History ใช้ข้อมูลจริง

**เดิม:** `mockHistory` 10 รายการเขียนตายในไฟล์

**ตอนนี้:** ดึงจาก `GET /api/history/logins`

น่าสนใจคือ backend บันทึกประวัติทุกครั้งที่ login อยู่แล้วตั้งแต่แรก แค่ไม่มีใครไปดึงมาแสดง

**ไฟล์:** `api.ts` (เพิ่ม `fetchLoginHistory`), `pages/HistoryPage.tsx`

---

## 3. ค่าเกณฑ์ในหน้า Settings บันทึกลงฐานข้อมูล

**เดิม:** ตัวเลขที่แสดง (25–32, 60–80) เขียนตายในโค้ด **ไม่ตรงกับฐานข้อมูล** ที่เก็บ 15–35 และ 30–90 อยู่จริง กดบันทึกก็ไม่ส่งไปไหน

**ตอนนี้:**
- เปิดหน้า → อ่านค่าจริงจาก `GET /api/thresholds`
- กดบันทึก → `PUT /api/thresholds`
- มีตรวจสอบ: ต้องเป็นตัวเลข, ต่ำสุดต้องน้อยกว่าสูงสุด
- ปุ่มขึ้น "กำลังบันทึก..." กันกดซ้ำ

**ไฟล์:** `api.ts`, `pages/SettingsPage.tsx`

---

## 4. ปั๊มน้ำ 1 ตัว สำหรับ 3 โซน

**เดิม:** แต่ละโซนมีแถบควบคุมปั๊มของตัวเอง (3 ชุด)

**ตอนนี้:** การ์ดปั๊มเดียวอยู่ด้านบนเหนือรายการโซน — ปุ่ม 4 โหมด (เปิด/ปิด/Auto/ตั้งเวลา) กับหน้าต่างตั้งเวลาเหมือนเดิม แค่เหลือชุดเดียว

การ์ดของแต่ละโซนเลยเหลือแค่ข้อมูลต้นไม้ ดูสะอาดขึ้น

**ไฟล์:** `pages/ZoneListPage.tsx`

> **หมายเหตุ:** ยังเป็น state ในหน้าเว็บ กดแล้วรีเฟรชกลับเป็น Auto — ยังไม่ได้ต่อ backend เพราะรอคำตอบเรื่องฮาร์ดแวร์ก่อน (ดูหัวข้อคำถาม)

---

## 5. ปุ่ม Export ใช้งานได้

**เดิม:** ปุ่มมีอยู่แต่กดแล้วไม่เกิดอะไร

**ตอนนี้:** ดาวน์โหลดไฟล์ `login-history-YYYY-MM-DD.csv` เปิดใน Excel ได้เลย (backend มี endpoint พร้อมอยู่แล้ว)

**ไฟล์:** `api.ts` (เพิ่ม `exportLoginHistory`), `pages/HistoryPage.tsx`

---

## 6. Logout บันทึกลงประวัติ

**เดิม:** ปุ่ม Logout ล้าง token ในเครื่องอย่างเดียว ไม่ได้แจ้ง backend → ตัวกรอง "Logout" ในหน้าประวัติจะว่างเปล่าตลอดกาล

**ตอนนี้:** เรียก `POST /api/auth/logout` ก่อนล้าง token

ถ้าต่อ backend ไม่ได้ ก็ยัง logout ฝั่งเราสำเร็จ (ไม่ค้างอยู่ในระบบ)

**ไฟล์:** `api.ts` (เพิ่ม `logout`), `App.tsx`

---

## 7. เก็บ IP และอุปกรณ์จริง

**เดิม:** backend hardcode `ip_address="127.0.0.1"` และ `device="local"` ทุกรายการ

**ตอนนี้:** เพิ่มฟังก์ชัน `_client_info(request)` ใน `app/routers/auth.py`
- อ่าน IP จาก request จริง
- รองรับ `X-Forwarded-For` เผื่อ deploy หลัง Nginx จะได้ IP ต้นทางจริง ไม่ใช่ IP ของ proxy
- แปลง User-Agent เป็นชื่อที่อ่านง่าย เช่น `Chrome / Windows`, `Safari / iOS`

ผลทดสอบ:
```
logout  success  192.168.1.55   Chrome / Windows   <- ใหม่
login   success  127.0.0.1      local              <- เก่า
```

**ไฟล์:** `backend/app/routers/auth.py` (ไฟล์เดียว)

> รายการเก่าที่บันทึกไปแล้วยังเป็นค่าเดิม เฉพาะรายการใหม่ที่จะเป็นของจริง

---

## 8. Settings บันทึกครบทุกช่อง

**เดิม:** refresh rate, เสียงแจ้งเตือน, แจ้งเตือนอีเมล — กดแล้วไม่มีผล

**ตอนนี้:** อ่านจาก `GET /api/settings` ตอนเปิดหน้า และบันทึกผ่าน `PUT /api/settings` พร้อมค่าเกณฑ์

ตอนนี้**หน้า Settings ทำงานจริงทุกช่องแล้ว**

**ไฟล์:** `api.ts`, `pages/SettingsPage.tsx`

---

## 9. กระดิ่งแจ้งเตือนใช้ข้อมูลจริง

**เดิม:** `systemAlerts` 5 รายการปลอมใน `Topbar.tsx` และ "อ่านแล้ว" หายเมื่อรีเฟรช

**ตอนนี้:** ดึงจาก `GET /api/alerts` — กดอ่านทีละอันหรือ "อ่านทั้งหมด" บันทึกลง DB จริง

### ⚠️ กระดิ่งจะว่างเปล่า

ตาราง `alerts` มี **0 แถว** เพราะยังไม่มีอะไรสร้าง alert เลย ต้องมี MQTT + ตัวตรวจค่าเกณฑ์ก่อน

กระดิ่งจึงขึ้น "✓ ไม่มีการแจ้งเตือนใหม่" ตลอด — **ถูกต้องตามความจริง** แต่ดูโล่งกว่าเดิม

ถ้าต้องการให้มีข้อมูลตอนนำเสนอ ใส่ตัวอย่างลง DB ได้โดยไม่ต้องแก้โค้ด:
```bash
cd Project/backend
python3 -c "
import sqlite3
c = sqlite3.connect('sensorhub.db')
c.execute(\"INSERT INTO alerts (tree_id,zone_id,type,message,is_read,created_at) VALUES (7,3,'critical','อุณหภูมิสูงเกินเกณฑ์ 36.8 องศา',0,datetime('now'))\")
c.commit()"
```

**ไฟล์:** `api.ts`, `components/Topbar.tsx`

---

## 10. ZoneDetail สอดคล้องกับปั๊มตัวเดียว

**ปัญหา:** ถ้ามีปั๊มตัวเดียว การสั่งเปิดปั๊มเฉพาะ "ต้นที่ 5" ย่อมเป็นไปไม่ได้ แต่หน้านี้ยังมีปุ่มควบคุมรายต้นอยู่

**ตอนนี้:** ลบปุ่มเปิด/ปิด/Auto ออก เหลือแสดงสถานะพร้อมข้อความ *"ปั๊มน้ำมี 1 ตัว ใช้ร่วมกันทุกโซน — ควบคุมได้ที่หน้า Zone List"*

**ไฟล์:** `pages/ZoneDetailPage.tsx`

---

## 11. Dashboard ต่อกับ backend

**เดิม:** อ่านจาก `mockData.ts` ทั้งหน้า และ `generateTodayStats()` สุ่มค่า min/max ใหม่ทุกครั้งที่รีเฟรช

**ตอนนี้:** ดึงจาก `GET /api/zones` + `GET /api/dashboard/summary`

ตัวเลขจริงที่ได้:
```
อุณหภูมิ  เฉลี่ย 31.0 C   ต่ำสุด 27.8   สูงสุด 36.8
ความชื้น  เฉลี่ย 69%      ต่ำสุด 58     สูงสุด 85
```

ตัวเลขจะ**นิ่ง** ไม่เปลี่ยนทุกครั้งที่รีเฟรชแล้ว และถ้า backend ไม่ได้รันจะขึ้น "เชื่อมต่อเซิร์ฟเวอร์ไม่ได้" แทนที่จะแสดงข้อมูลปลอม

**ไฟล์:** `api.ts` (เพิ่ม `fetchZones`, `fetchDashboardSummary`), `pages/DashboardPage.tsx`

---

## ไฟล์ที่แตะทั้งหมดวันนี้

**Frontend** (`Project/frontend/src/`)
| ไฟล์ | หมายเหตุ |
|---|---|
| `api.ts` | ศูนย์รวมการเรียก backend — โตขึ้นมากวันนี้ |
| `App.tsx` | logout แจ้ง backend |
| `components/Topbar.tsx` | กระดิ่งข้อมูลจริง |
| `pages/DashboardPage.tsx` | ต่อ backend |
| `pages/HistoryPage.tsx` | ข้อมูลจริง + Export |
| `pages/SettingsPage.tsx` | ตัดภาษา, บันทึกครบ |
| `pages/ZoneListPage.tsx` | ปั๊มรวม |
| `pages/ZoneDetailPage.tsx` | ลบปุ่มปั๊มรายต้น |

**Backend**
| ไฟล์ | หมายเหตุ |
|---|---|
| `app/routers/auth.py` | เก็บ IP/อุปกรณ์จริง — **ไฟล์เดียวที่แตะฝั่ง backend** |

**ไม่ได้แตะเลย:** `models.py`, `schemas.py`, `database.py`, `config.py`, router อื่น ๆ, โครงสร้างฐานข้อมูล

---

## ยังเหลืออะไร

### งานใหญ่ — เป็นเรื่องเดียวกัน

```
ESP8266 --> MQTT --> backend --> ฐานข้อมูล --> หน้าเว็บ
             ^ ขาดตรงนี้                        ^ และบางส่วนตรงนี้
```

| # | เรื่อง | สภาพ |
|---|---|---|
| A | **MQTT** | `dependency/mqtt_client.py` มีแค่ 2 บรรทัดที่เป็น comment |
| B | **ZoneList / ZoneDetail ต่อ backend** | ยังใช้ `mockData.ts` (Dashboard เสร็จแล้ว) |

พอทำ 2 อย่างนี้เสร็จ อีก 2 เรื่องจะตามมาเอง:
- ค่าเกณฑ์ที่ตั้งไว้จะมีผลกับสีสถานะจริง (ตอนนี้ตั้งได้แต่ยังไม่มีใครเอาไปใช้)
- กระดิ่งจะมีแจ้งเตือนโผล่มาเอง

### งานโครงสร้าง

| # | เรื่อง |
|---|---|
| C | ฐานข้อมูลเป็น SQLite ไม่ใช่ MySQL — `config.py` ไม่เคยอ่าน `DB_HOST/DB_USER/DB_PASSWORD` ใน `.env` |
| D | repo 315MB เพราะ `node_modules` + `.venv` ถูก commit (ไม่มี `.gitignore`) |
| E | ค่าเกณฑ์แยกรายโซน — ทำไว้แล้วแต่**พักไว้** รอตัดสินใจ (ดูข้อกังวลด้านล่าง) |

---

## ข้อกังวล

### 1. ค่าเกณฑ์รายโซนติดที่โครงสร้างฐานข้อมูล

ตาราง `thresholds` ใช้ `parameter` เป็น primary key เดี่ยว:
```python
class Threshold(Base):
    parameter = Column(String(32), primary_key=True)
```

แปลว่า `temperature` มีได้แถวเดียวทั้งระบบ ไม่มีช่องระบุโซน

**ทางที่ถูกต้อง:** เพิ่มคอลัมน์ `zone_id` แล้วเปลี่ยน primary key เป็นคู่ `(parameter, zone_id)` — แต่ SQLite แก้ primary key ไม่ได้ ต้องสร้างตารางใหม่แล้วย้ายข้อมูล = เปลี่ยนโครงสร้างที่ทีมใช้ร่วมกัน

**ทางเลี่ยง (ทำไว้แล้วแต่ยังไม่ได้ใช้):** ใช้ชื่อพารามิเตอร์แยกโซน `temperature_z1`, `humidity_z2` ซึ่ง router เดิมรองรับอยู่แล้วโดยไม่ต้องแก้ backend เลย แต่ชื่อแบบนี้ไม่สวยตามหลักฐานข้อมูล

**ยังไม่ตัดสินใจ** รอคุยกัน

### 2. ไฟล์ `.db` อยู่ใน git

`sensorhub.db` เป็นไฟล์ binary ที่ git รวมให้ไม่ได้ ถ้าเราสองคนแก้ข้อมูลคนละเครื่องแล้ว push ทั้งคู่ จะเกิด conflict ที่แก้ได้ทางเดียวคือ**เลือกไฟล์ของใครคนหนึ่งทั้งก้อน** อีกคนเสียข้อมูลหมด

เสนอให้ตกลงกันว่าใครเป็นคนอัปเดต `.db` หรือบอกกันก่อนทุกครั้ง

### 3. `__pycache__` ติดขึ้น repo ทุกครั้งที่ commit

ไฟล์ `.pyc` ถูก track อยู่ตั้งแต่แรก ทำให้ทุก commit มีไฟล์ขยะปนไปด้วย (commit ล่าสุดมี 21 ไฟล์ แต่เป็น `.pyc` ซะเยอะ)

แก้ได้ด้วย `.gitignore` + `git rm --cached` แต่กระทบทั้งทีม เลยยังไม่ทำ

### 4. เรื่องความปลอดภัย (ไม่ด่วน)

`app/config.py` มีค่าเริ่มต้นเขียนไว้ในโค้ดที่อยู่บน public repo:
```python
JWT_SECRET = os.getenv("JWT_SECRET", "dev-secret-change-me")
DEFAULT_ADMIN_PASSWORD = os.getenv("DEFAULT_ADMIN_PASSWORD", "Admin@1234")
```
และเพราะ `.env` มีแต่ค่า placeholder ระบบจึงใช้ค่า default พวกนี้จริง ๆ

ตอนนี้ไม่เป็นไรเพราะรันแค่ในเครื่อง แต่**ถ้าจะ deploy ขึ้นอินเทอร์เน็ตต้องแก้ก่อน**

### 5. กฎตรวจสอบ username ยังไม่ได้ทำ

โจทย์ตั้งต้นที่พี่เคยบอกว่า "เปลี่ยนเป็น `str` แล้วแต่ยังใส่ตัวเลขได้"

สาเหตุที่แท้จริงเจอแล้ว (อธิบายละเอียดใน `Bank_CHANGES_TH.md`): **หน้า Login เดิมไม่เคยเรียก backend เลย** จึงแก้ `schemas.py` เท่าไหร่ก็ไม่มีผล — ตอนนี้ต่อแล้ว แก้แล้วจะเห็นผลจริง

แต่ยังไม่ได้ใส่กฎ เพราะยังไม่รู้ว่าต้องการกฎแบบไหน (ดูคำถามข้อ 4)

---

## คำถามถึงพี่

### 1. ฮาร์ดแวร์มีวาล์วแยกแต่ละโซนไหม?

มีผลกับเรื่องปั๊มโดยตรง:
- **ปั๊มเดียว ไม่มีวาล์ว** → เปิดทีเดียวรดหมด 3 โซน ถ้าโซน 1 ดินแห้งแต่โซน 3 ดินแฉะ ระบบอัตโนมัติจะตัดสินใจยาก
- **มีวาล์วแยกโซน** → ควบคุมรายโซนได้ แค่เปลี่ยนคำเรียกจาก "ปั๊ม" เป็น "วาล์ว" แล้วมีปั๊มหลักคุมอีกชั้น

ตอนนี้ทำเป็นปั๊มเดียวไว้ก่อน ถ้ามีวาล์วต้องปรับใหม่

### 2. ESP8266 ส่งข้อมูลอะไรได้บ้าง?

**ข้อนี้สำคัญที่สุด** เพราะกำหนดว่า MQTT จะรับอะไรและตาราง `sensor_readings` เก็บอะไร

ตาราง `thresholds` มี 5 พารามิเตอร์อยู่แล้ว: `temperature`, `humidity`, `light`, `soil_moisture`, `ph` — แต่หน้าเว็บใช้แค่ 2 ตัวแรก อีก 3 ตัวมีในฐานข้อมูลแต่ไม่มี UI ให้ตั้ง

ต้องรู้ว่าเซนเซอร์จริงวัดอะไรได้บ้าง จะได้ทำ UI ให้ตรง

### 3. ค่าเกณฑ์ต้องแยกรายโซนจริงไหม?

ถ้าแต่ละโซนสภาพต่างกันจริง (อยู่ใต้ร่มเงา/โดนแดดจัด, ต้นอายุต่างกัน) การแยกก็สมเหตุสมผล แต่ต้องแก้โครงสร้างฐานข้อมูล (ดูข้อกังวลข้อ 1)

ถ้าใช้ชุดเดียวทั้งสวนพอ ก็ไม่ต้องทำอะไรเพิ่ม

### 4. กฎตรวจสอบ username อยากได้แบบไหน?

คำถามที่ต้องตอบก่อนคือ **"ตัวเลขเข้าได้แล้วเป็นปัญหาตรงไหน"** เพราะระบบส่วนใหญ่อนุญาตให้มีตัวเลขในชื่อผู้ใช้

| แบบ | อนุญาต | ปฏิเสธ |
|---|---|---|
| เข้มสุด: ตัวอักษรล้วน | `admin`, `somchai` | `user01`, `12345` |
| **มาตรฐาน** (แนะนำ) | `admin`, `farmer01` | `12345`, `a b`, `ad!` |
| หลวม: ห้ามช่องว่าง/อักขระพิเศษ | เกือบทุกอย่าง | `a b`, `<script>` |

แบบมาตรฐานใช้ `^[a-zA-Z0-9_]{3,20}$` + ต้องมีตัวอักษรอย่างน้อย 1 ตัว → กัน `12345` ได้ แต่ยังใช้ `farmer01`, `staff02` ได้

> **รหัสผ่านห้ามจำกัดตัวเลขเด็ดขาด** จะทำให้รหัสอ่อนลงมาก และรหัสปัจจุบัน `Admin@1234` จะใช้ไม่ได้ทันที

### 5. จะย้ายไป MySQL ไหม?

ถ้าเป็นข้อบังคับของงาน ต้องแก้ `config.py` ให้ประกอบ `DB_URL` จากตัวแปรใน `.env` + ติดตั้ง driver (`pymysql`)

ข่าวดีคือ `database.py` รองรับไว้แล้ว และ SQLAlchemy เป็นตัวกลาง — `models.py` กับ router ทั้งหมด**ไม่ต้องแก้สักบรรทัด**

### 6. จะถอด `node_modules` กับ `__pycache__` ออกจาก repo ไหม?

จะทำให้ repo เล็กลงจาก 315MB เหลือไม่กี่ MB และ commit สะอาดขึ้นมาก แต่หลังจากนั้นทุกคนต้อง `npm install` เอง

---

## วิธีรัน (ย่อ)

ต้องเปิด **2 terminal** พร้อมกัน

```bash
# terminal 1 — backend
cd <path>/Project/backend
source venv/bin/activate
python -m uvicorn app.main:app --port 8000

# terminal 2 — frontend
cd <path>/Project/frontend
npm run dev
```

เปิด http://localhost:5173 — login ด้วย `admin` / `Admin@1234`

> ตอนนี้**ต้องเปิด backend ด้วยเสมอ** ไม่งั้น Dashboard, History, Settings จะขึ้น "เชื่อมต่อเซิร์ฟเวอร์ไม่ได้"
>
> วิธีติดตั้งครั้งแรกดูใน `Bank_CHANGES_TH.md`
